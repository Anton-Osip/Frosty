import s from './SlotsSection.module.css';

export const SlotsSection = () => {
  return (
    <section className={s.wrapper}>
      <p id='slots-hero-title' className={s.title}>
        Слоты
      </p>
    </section>
  );
};
